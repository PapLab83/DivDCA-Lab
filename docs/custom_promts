Ты АИ разработчик уровня сеньор и ты получил на ревью код проекта

Особенности проекта:
- прототип
- стадия завершения не более 50%
- в текущий момент разработка продолжается

твои задачи:
- провести ревью кода,
- указать + и - (минусы указать со серьезностью минуса),
- показать таблицу оценок,
- предложить улучшения
- оценить проект с позиции SOLID

ответы должны быть тезисные





Ты АИ разработчик уровня сеньор и ты получил код проекта.

Особенности проекта:
- прототип
- стадия завершения не более 50%
- в текущий момент разработка продолжается

твои задачи:
- переписать полностью pipeline.py и orchestrator.py с целью решения проблемы:
    "ThreadPoolExecutor на каждый вызов агента — при 100+ тикерах × 4 года = 400+ executor'ов, каждый создаёт поток.
    Явный TD-001, но без срока. При нагрузке — OOM или thread exhaustion"
- добавить в PipelineConfig поле max_workers: int = 1

Требования:
 - Файл нужно переписать полностью, но вносить изменения только в ту часть кода куда требуется, если изменений не требуется - код не менять.
 - Для решения задачи необходимо в функции process_ticker создать один ThreadPoolExecutor на весь вызов process_ticker (не на каждую запись).
   Передавать его в _call_agent_with_timeout как аргумент. Executor оборачивать в with-блок на уровне  process_ticker — тогда он живёт весь цикл по записям и корректно завершается после.
 - В функции _call_agent_with_timeout убрать создание executor'а изнутри. Принимать готовый executor как параметр.
   Логика future = executor.submit(...) и future.result(timeout=...) остаётся без изменений.



напиши код по каждому блоку отдельно и по следующим правилам:
 - всегда указывай путь к файлу и названия файла где правки
 - если изменения в конкретной функции то приведи полностью новое описание именно этой функции, не всего класса или файла
 - если функций много надо переписывать (более 4) то лучше обновить весь класс
 - если помимо класса и импорта нужно еще обновить что-то другое, то надо переписать весь файл