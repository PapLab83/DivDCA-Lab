отлично, спапсибо
теперь смотри мне надо приостановить развитие продукта на некоторое время, но я могу к нему возвращаться, поэтому хорошо иметь план развития проекта. давай его для начала составим


я тебе напишу ниже что считаю надо сделать, ты из этого составь короткий план - по сути туду лист с пояснениями

0) (+) Разнести код по директориям
1) (+) Сохранять только итоговую таблицу в ексель (не надо проверки и ссв)
2) (+) Изменить названия директорий для данных
2) (+) Сделать скрипт который переводит файлы с яхо в с джейсон
   2) (+) цены на акции
   3) (+) дивиденды
2) (+) структурировать этапы ETL
2) (+) структурировать директории данных
2) (+) структурировать скрипты исполнения
2) (+) инициализировать скрипты по этапу преобразования данных
2) (+) Сделать с помощью дипсик годовые записи событий по имеющимся бумагам (как сейчас события лонг и шот в джейсон)
3) (+) Сборка цен акций, дивидендов и событий в одну таблицу (с учетом правил заложенной логики)
4) (+) Выбор провайдера
5) (-) Агент генерации новостей
   5) (+) Промпт
   5) (+) Архитектура: директория, название, ...
   5) (-) Минимально тестовое решение
         (+) Базовый класс агента
         (+) Рефакторинг базового класса агента
         (+) Адаптер ЛЛМ
         (+) Фабрика (core/agent_factory.py)
         (+) Реестр агентов (core/agent_registry.py)
         (+) Разнести агентов и МАС 
         (+) Продумать архитектуру и функционал с возможностью выноса МАС в отдельный сервис в будущем:
                + отсутствие связей агентов например с ETL напрямую
                + сделать тонкий слой инструментов (tools abstraction), в итоге агент будет знать про Tool, а не про ETL
     (+) Дублирование в AgentRegistry (def get и def get_info - одно и тоже)
     (+) Нет async поддержки: AsyncLLMAdapterProtocol объявлен, но нигде не используется. BaseAgent.execute() — синхронный. Для I/O-bound LLM-вызовов это bottleneck.
     (+) Исправить импорты → единый стиль
     (+) Заглушки для пустых engine'ов
     (+) Защитить API ключ
     (+) Автоматическая регистрация агентов через декоратор
     (+) Использовать dataclasses.replace() вместо пересоздания
     (+) Retry через tenacity вместо ручного цикла
     (+) Добавить минимальный тест
     (+) Конфигурация из env / yaml
     (+) Описать архитектуру
     (+) Написать ридми агентов
     (+) Ревью: cache.py
     (+) Ревью: execute & execute_async in BaseAgent
     (+) Ревью: acall for adapter.py & base_agent.py
     (+) Ревью: agent_factory.py
     (+) Ревью: agent_validator.py
     (+) Ревью: config.py
     (+) Ревью: test_core.py
     (+) Ревью: баги
         + Убрать singleton из AgentFactory → обычный класс, инжектируемый через конструктор
         + Честная async-реализация по умолчанию
         + Двойная регистрация агентов
         + Документировать ограничение Claude/Gemini — либо убрать пустые алиасы, либо явно написать что нужен OpenAI-compatible прокси (LiteLLM, OneAPI)
         + Удалить пустые файлы или заменить на raise NotImplementedError("TODO: ...") с docstring
     (+) Базовый движок модели (core/llm/engines/base_engine.py)
     (+) Реализация OpenAI (openai_engine.py)
     (+) Менеджер сборки промпта (core/prompt_manager.py)
   5) (+) Запустить pytest agents/tests/test_core.py -v
   6) (+) Агент получения событий (agent.py)
   5) (+) Оркестратор 
   5) (+) Пайплайн 
   5) (+) Ранер
   5) (-) Ревью агента, оркестратора, пайплайна и ранера
   5) (-) Ран с обращением к ЛЛМ
   5) (-) Скилы и инструменты: по 1/2 для обкатки
   5) (-) All engines
   5) (-) Валидация результатов
   5) (-) Определить подход использования инструментов: детерминированный, агент сам решает, гибридный
6) (-) Сделать учет инвестиций ежемесячным
6) (-) Провести детальное тестирование расчетов
6) (-) Сформировать диписиком годовые таблицы событий по 40+ акциям
7) (-) Добавить показатели финансовой отчетности (квартал) и сформировать показатели которые будут помогать определять действие: покупка / продажа
 - быстро с яхо финанс  
 - дольше с опенсорсными специализированными фреймворками  
8) (-) Добавить функционал моделирования портфеля
 - просто сделать: для каждой акции периоды инвестирования, суммы инвестирования, суммы продаж акций и получения валюты (вероятно)
 - сложнее  реализовать: фактический учет портфеля по каждой сделке, транзакции внесения и вывода валюты и тд (вероятно нужны коннекторы и парсеры для брокеров)
6) (-) Для удобства юзера реализовать возможность запуска прототипа с удаленной машины через докер. 
Как это выглядит для юзера.Он переходит по ссылке на страницу. 
На странице можно выбрать бумагу, период и ежемесячную сумму инвестирования и получить таблицу в екселе и график цены с описанием событий 
(при наведении курсором на год и цену появляется в сплывающем окне описание события где заголовок - шот, текст - лонг.
